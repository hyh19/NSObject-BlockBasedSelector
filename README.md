NSObject+BlockBasedSelector
==========================

Category on NSObject which allows the equivilant of performSelector:afterDelay to be executed in a block.

## How to use


```objc

[self performBlock:^{

//Code

} afterDelay:2.5];
```
